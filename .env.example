DATABASE_URL="postgresql://mun@localhost:5432/qubit?schema=public"
PORT=4000
PUBLIC_API_BASE_URL="http://localhost:4000"
PUBLIC_WEB_BASE_URL="http://localhost:3000"

# console: print mail payloads to the API server log
# smtp: send mail through SMTP/Postfix
MAIL_PROVIDER="console"
MAIL_FROM="Q-ubit <no-reply@q-ubit.local>"

# Postfix local SMTP example
SMTP_HOST="localhost"
SMTP_PORT=25
SMTP_SECURE=false

# External SMTP providers may require auth.
# SMTP_USER=""
# SMTP_PASS=""
